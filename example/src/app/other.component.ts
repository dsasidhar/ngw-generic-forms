import { Component, ViewChild, AfterViewInit } from '@angular/core';
import { Validators } from '@angular/forms'

@Component({
  selector: 'other',
  template: `
  Some random component, just to test unloading of app component
  `
})
export class OtherComponent {
  
}
